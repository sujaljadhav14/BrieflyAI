[project]
name = "text-summarizer-backend"
version = "0.1.0"
description = "FastAPI backend for dialogue summarization using T5"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.30.0",
    "torch>=2.4.0",
    "transformers>=4.40.0",
    "sentencepiece>=0.2.0",
    "pydantic>=2.0.0",
]
